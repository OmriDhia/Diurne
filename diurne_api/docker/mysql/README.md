# MySQL Database Configuration

## Directory Structure
- `conf/`: MySQL configuration files
- `initdb/`: Initialization scripts (tracked in Git)
- `dumps/`: Database dump files (not tracked in Git)

## Restoring a database
Place your SQL dump files in the `dumps/` directory and rebuild the container.